import React from 'react';

const KediUserDashboard = () => {
  // Temporarily simplified to avoid initialization errors
  return (
    <div className="min-h-screen bg-[#f8f9fa] font-['Poppins',sans-serif] scroll-smooth">
      <div className="p-6">
        <h1 className="text-2xl font-bold text-gray-900">Dashboard</h1>
        <p className="text-gray-600 mt-2">Dashboard temporarily disabled due to initialization errors. Will be restored once issues are resolved.</p>
      </div>
    </div>
  );
};

export default KediUserDashboard;